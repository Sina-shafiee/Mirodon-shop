import HeroCarousel from './HeroCarousel';

// hero section component
const Hero = () => {
  return (
    <section className='mt-4 md:mt-6'>
      <HeroCarousel />
    </section>
  );
};

export default Hero;
